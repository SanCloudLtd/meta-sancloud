header:
  version: 8
  includes:
    - bbe-poky.yml

repos:
  meta-kernel:
    url: https://gitlab.com/openembedded/community/meta-kernel.git
    refspec: master

local_conf_header:
  linux_kernel: |
    BBE_KERNEL_PROVIDER = "stable"
