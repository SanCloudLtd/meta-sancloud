header:
  version: 9

local_conf_header:
  debug_tools: |
    CORE_IMAGE_EXTRA_INSTALL += " \
        lsof pstree util-linux vim strace python3 bash devmem2 \
        htop iotop iftop \
        openssh rsync iperf3 tcpdump netcat \
        "
